import React from 'react';
import styles from './footer.module.css';

const Footer = () => (
    <footer className={styles.footer}>
        <p>Completely protect your information.</p>
    </footer>
);

export default Footer;